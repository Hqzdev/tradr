# Auth application

`AuthService` implements registration, login, token refresh, logout, and current-user retrieval. It coordinates domain objects, repositories, password hashing, and token services without depending on HTTP classes.
