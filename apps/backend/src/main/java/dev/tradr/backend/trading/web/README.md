# HTTP-слой торговли

`TradingController` предоставляет только чтение заявок, сделок, статистики и
совместимого портфеля. Создание, preview и отмена ручных заявок не опубликованы.
Агентские операции выполняются внутри `TradingService`.
