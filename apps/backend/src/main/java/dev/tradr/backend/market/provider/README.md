# Market provider

`SyntheticMarketDataProvider` creates deterministic historical candles and the repeatable live tick scenario. A later real provider can implement the same interface without changing the application or transport layers.
