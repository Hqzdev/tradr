# HTTP-слой торговли

`TradingController` предоставляет API для предварительного расчёта, создания и отмены заявок, истории сделок и портфеля. Контроллер не считает деньги сам: он передаёт работу `TradingService`.
